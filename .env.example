# Copy this file to .env and fill in the values
# For local development without a DATABASE_URL, PGLite is used automatically

# Required for production (Neon serverless Postgres)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Local dev server port (optional, defaults: API=3000, frontend=5173)
PORT=3000
NODE_ENV=development
